import * as React from 'react';

import './Spinner.scss';

// eslint-disable-next-line react/function-component-definition
const Spinner: React.FunctionComponent = () => (<div className="loader">Loading...</div>);

export default Spinner;
